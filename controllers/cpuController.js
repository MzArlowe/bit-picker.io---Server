//stretch goal: add a button to the cpu component to set the cpu to a boolean value of amd or intel (amd = true? : false)
// const { DataTypes } = require("sequelize");
// const router = require("express").Router();
// const db = require("../db");


